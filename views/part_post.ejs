<div username="<%= post.name %>">
	<%= post.name %><button class="btn btn-default follow-button" username="<%= post.name %>">follow</button>
	<img src="<%= post.image_url %>">
	<p><%= post.text %></p>
</div>